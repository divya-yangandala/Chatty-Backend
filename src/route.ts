import { Application } from "express"

export default (app: Application) => {
    const routes = () =>{
        //app.use('/api/v1') we will call the route using middleware app.use
    };
    routes();
}